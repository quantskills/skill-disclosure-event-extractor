# Python 3.9+
# extract_events / validate_events are pure-stdlib+pandas and run fully offline.
# requests is needed only by fetch_announcements.py (公告抓取) and
# backtest_events.py (行情). pdfplumber is optional: skip it with --no-text,
# which classifies from titles only and never opens a PDF.
pandas>=1.3
requests>=2.28
# optional
# pdfplumber>=0.9      # PDF 正文抽取；--no-text 时不需要
# pyarrow>=8.0         # parquet I/O
